<%= form_for([@dpto, @subdpto], :remote => true) do |f| %>
  <% if @subdpto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subdpto.errors.count, "error") %> prohibited this subdpto from being saved:</h2>

      <ul>
      <% @subdpto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <thead>
      <tr>
        <th>Subdepartamento</th>
        <th>Sigla</th>
      </tr>
    </thead>
    <tbody>
      <tr class="fields">
        <td><%= f.text_field :name, :autofocus => true %></td>
        <td><%= f.text_field :abbr %></td>
        <td><%= f.submit "Salvar" %></td>
      </tr>
    </tbody>
  </table>
<% end %>
